# Planning Dashboard Environment Variables

# Cron Job Secret (optional, for security)
# Generate a random secret and set it here
CRON_SECRET=your-secret-key-here

# Planning Directory (local development only)
# In production, data comes from webhook/sync
PLANNING_DIR=/home/bean/Development/planning

# Vercel Webhook URL (for local sync script)
# The deployed URL of your webhook endpoint
VERCEL_WEBHOOK_URL=https://your-app.vercel.app/api/webhook/sync
